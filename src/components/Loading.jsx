const Loading = () => {
    return (
        <div className="w-full flex justify-center items-center animate-pulse p-10">
            <h2 className="animate-pulse text-gray">Loading...</h2>
        </div>
    );
}
 
export default Loading;